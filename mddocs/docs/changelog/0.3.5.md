# 0.3.5 (2026-07-22) { #0.3.5 }

## Improvements

- Improved full-text search ([#255](https://github.com/MTSWebServices/syncmaster/issues/255))

  Refine `tsvector` generation and adding better tokenization for technical fields such as hostnames, table names, and directory paths.

- Set `JAVA_HOME` if not set in worker entrypoint.

- Make `config.yml` priority higher than environment variables.
