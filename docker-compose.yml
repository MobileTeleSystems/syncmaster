version: '3.8'
services:
  backend:
    image: syncmaster_back
    restart: unless-stopped
    build:
      dockerfile: docker/backend.dockerfile
    ports:
      - 8000:8000
    networks:
      - network

networks:
  network:
